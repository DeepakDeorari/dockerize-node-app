1. Created a simple node app ("Hello World") API.
2. Then Created a Dockerfile to dockerize the app and it is listening to port 3000
3. Having an Ansible playbook to deploy on centos(may be any public cloud).
